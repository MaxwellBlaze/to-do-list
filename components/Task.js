import React from 'react';
import { StyleSheet, Text, View, Image, TouchableOpacity  } from 'react-native';

const Task = ({id, name, priority, timeDate, timeToComplete, isCompleted, belongsTo, dateCreated}) => {

    return(
        <View>
            
        </View>
    )
};

export default Task;